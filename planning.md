# Plan Mode

List all **write operations** in every plan:
- Every non-read-only command and what it changes
- No vague steps like "clean up the branch"
- Destructive commands: list them but don't run
