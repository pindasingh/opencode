# Post-Change Checks

After completing a feature or other meaningful unit of work (not every tiny change), run these in order:

1. Build — confirm it compiles/packages.
2. Run tests — execute the relevant test suite(s).
3. Format — apply existing formatter rules for the language.
